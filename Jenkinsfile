pipeline{
    agent any 

    environment {
        OWNER : "OUMAR"
    }

    stages{
        stage("build de l'application"){
            steps{
                echo "BRANCH_NAME :  ${BRANCH_NAME}"
                echo "OWNER : ${OWNER}"
            }
        }
    }
}